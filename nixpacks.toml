[phases.setup]
nixPkgs = ["python312", "playwright-driver"]

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["cd backend && playwright install --with-deps chromium"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
